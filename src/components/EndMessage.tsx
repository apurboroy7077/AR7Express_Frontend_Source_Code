const EndMessage = () => {
  return (
    <div className="text-xs text-center p-1 bg-slate-300 mt-2 mb-2">
      You've reached the end. Keep exploring!
    </div>
  );
};

export default EndMessage;
